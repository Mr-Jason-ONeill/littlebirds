<footer>
    <div class="footer-grid">
      <div class="grid-item">
        <h1>{{ shop.name }}</h1>
        </div>
      <div class="grid-item">
        <div>Love. Laughter. Legacy.</div>
      </div>
      <div class="grid-item">
        <button>Shop Now</button>
      </div>
      </div>
    </div>
    <nav>
      <ul class="footer-links">
        {% for link in linklists.main-menu.links -%}
          <li class="footer-item">{{ link.title | link_to: link.url }}
          </li>
        {%- endfor %}
      </ul>
    </nav>
</footer>