<header>
  <div class="logo">
    <h1>{{ shop.name }}</h1>
    <div>Logo</div>
  </div>
  <nav>
    <ul>
      {% for link in linklists.main-menu.links -%}
        {{ link.title | link_to: link.url }}
      {%- endfor %}
    </ul>
  </nav>
</header>